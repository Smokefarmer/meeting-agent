/**
 * End-of-meeting summary generator.
 * Issue #6 — NostraAIAgent.
 * Stub — full implementation in feature/issue-6-summary branch.
 */

import type { MeetingSession } from './session.js';

export async function generateAndSendSummary(
  _session: MeetingSession,
): Promise<void> {
  // TODO: Issue #6 — build markdown summary, save locally, send via Telegram
  throw new Error('Not implemented — see Issue #6');
}
